{
  "name": "angularwp-starter",
  "version": "0.0.1",
  "description": "Angular x WP-API starter kit.",
  "main": "index.html",
  "scripts": {
    "start": "browser-sync start --config bs-config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^1.3.7"
  }
}
